# ─── Database ─────────────────────────────────────────────
# PostgreSQL connection string
# For Docker: postgresql://nebula:nebula@localhost:5432/nebula
DATABASE_URL="postgresql://nebula:nebula@localhost:5432/nebula"

# ─── Auth (Better Auth) ──────────────────────────────────
# Secret key for encrypting sessions (generate with: openssl rand -base64 32)
BETTER_AUTH_SECRET="change-me-to-a-random-secret"

# Public URL of the application (used for OAuth callbacks)
BETTER_AUTH_URL="http://localhost:3000"

# ─── OAuth Providers (optional) ──────────────────────────
# GitHub OAuth -- https://github.com/settings/developers
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# Google OAuth -- https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# ─── App ──────────────────────────────────────────────────
# Public URL exposed to the browser (must match BETTER_AUTH_URL in most setups)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ─── Email (optional -- for password reset, team invites) ─
# Resend API key -- https://resend.com
RESEND_API_KEY=""

# Or use generic SMTP:
# SMTP_HOST=""
# SMTP_PORT=587
# SMTP_USER=""
# SMTP_PASS=""
# SMTP_FROM="noreply@example.com"

# ─── Docker / Production ─────────────────────────────────
# Set to "production" when deploying
NODE_ENV="development"

# Port the app listens on inside Docker
PORT=3000
